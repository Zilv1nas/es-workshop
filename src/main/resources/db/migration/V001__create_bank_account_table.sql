create table bank_account_projection
(
    id                                text                     primary key,
    name                              text                     not null,
    currency                          text                     not null
);